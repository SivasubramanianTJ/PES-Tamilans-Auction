info:
  name: Excel Test
  type: http
  seq: 2

http:
  method: GET
  url: import.meta.env.VITE_API_URL/api/export/excel
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
