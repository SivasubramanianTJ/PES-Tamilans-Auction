info:
  name: Create Player
  type: http
  seq: 1

http:
  method: POST
  url: import.meta.env.VITE_API_URL/api/players
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "name": "Virat Kohli",
        "phoneNumber": "9876543210"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
