info:
  name: Get Current User
  type: http
  seq: 2

http:
  method: GET
  url: import.meta.env.VITE_API_URL/api/auth/me
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
