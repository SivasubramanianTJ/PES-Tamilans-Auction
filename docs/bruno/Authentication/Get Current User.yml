info:
  name: Get Current User
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:4000/api/auth/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
