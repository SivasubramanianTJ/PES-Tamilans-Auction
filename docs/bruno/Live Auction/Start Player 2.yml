info:
  name: Start Player 2
  type: http
  seq: 7

http:
  method: POST
  url: import.meta.env.VITE_API_URL/api/live-auction/start-player
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "seasonPlayerId": "64eafeb4-3384-465f-b818-ed1b125ae0eb"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
