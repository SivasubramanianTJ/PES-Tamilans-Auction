info:
  name: Start Player
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:4000/api/live-auction/start-player
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "seasonPlayerId": "64eafeb4-3384-465f-b818-ed1b125ae0eb"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
