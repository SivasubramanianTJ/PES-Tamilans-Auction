info:
  name: Analytics 1
  type: http
  seq: 13

http:
  method: GET
  url: import.meta.env.VITE_API_URL/api/analytics
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
