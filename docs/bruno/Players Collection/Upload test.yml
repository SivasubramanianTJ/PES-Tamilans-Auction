info:
  name: Upload test
  type: http
  seq: 7

http:
  method: POST
  url: import.meta.env.VITE_API_URL/upload-test
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/sivasubramanian/Desktop/players.xlsx
        contentType: File
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
