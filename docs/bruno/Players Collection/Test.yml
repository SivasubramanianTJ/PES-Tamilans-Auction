info:
  name: Test
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:4000/api/teams/live
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
