info:
  name: Test
  type: http
  seq: 11

http:
  method: GET
  url: import.meta.env.VITE_API_URL/api/teams/live
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
