info:
  name: Player Image Test
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:4000/api/live-auction/current
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
