info:
  name: Rwaw Test
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:4000/api/players/raw-test
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/sivasubramanian/Desktop/players.xlsx
        contentType: File
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
