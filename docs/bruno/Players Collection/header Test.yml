info:
  name: header Test
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:4000/api/players/headers-test
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/sivasubramanian/Desktop/players.xlsx
        contentType: FIle
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
