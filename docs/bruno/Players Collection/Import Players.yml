info:
  name: Import Players
  type: http
  seq: 1

http:
  method: POST
  url: import.meta.env.VITE_API_URL/api/players/import
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
