info:
  name: Import Players
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4000/api/players/import
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
