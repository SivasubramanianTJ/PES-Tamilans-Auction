info:
  name: Test Image Players
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:4000/api/players
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
